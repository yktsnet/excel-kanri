## モバイル幅でのレイアウト崩れを修正する
id: 08
branch-slug: responsive-layout
github_issue:
status: draft
type: fix
対象: frontend/src/App.tsx, frontend/src/components/PdfPreview.tsx
内容: `App.tsx` のファイル一覧/PDFプレビューの2カラムグリッドが `grid-cols-[320px_1fr]` で固定幅のためレスポンシブ対応しておらず、スマホ幅では右側のPDFプレビュー領域が極端に狭くなり崩れる（`PdfPreview.tsx` の「ファイルを選択してください」というプレースホルダーが1文字ずつ縦積みで折り返される）。viewer・editor 両ロールで同じレイアウトを共有しているため、どちらのロールでも崩れる。狭幅では1カラム縦積みに切り替える。
確認: 目視確認（ブラウザのビューポート幅を狭めて viewer ロール・editor ロールそれぞれでファイル一覧とPDFプレビューが縦積みで崩れずに表示されることを確認する。editor ロールは追加で「新規作成」ボタンと `GenerateModal` の表示も崩れないことを確認する）

---

### 保証
- 新たに宣言する保証: なし（見た目の修正であり、明文化して約束する挙動は増えない）
- 維持する保証: なし（`docs/guarantees.md` の対象は `packages/` 配下のみで、フロントエンドの見た目に既存の保証記載はない。既存の PC 幅（1280px 相当）でのレイアウトは崩さない）

### 詳細

#### `frontend/src/App.tsx`
- 78行目 `grid-cols-[320px_1fr]` を `grid-cols-1 md:grid-cols-[320px_1fr]` のようにブレークポイント付きに変更し、`md` 未満では1カラム縦積みにする
- 65行目 `p-8` を `p-4 md:p-8` のように狭幅で余白を減らす
- 縦積み時、ファイル一覧側（79〜98行目のブロック）とPDFプレビュー側（99行目 `PdfPreview`）にそれぞれ十分な高さが割り当たるか確認し、必要なら `min-h-0` に加えて明示的な高さ（例: `h-[50vh]`）を検討する

#### `frontend/src/components/PdfPreview.tsx`
- 親のレイアウト変更後、未選択時のプレースホルダー（16〜22行目）とiframe表示（24〜44行目）が縦積みレイアウト下で潰れずに表示されることを確認し、必要な場合のみ高さ関連のクラスを調整する

#### 実装順序
1. `App.tsx` のグリッド・パディングをレスポンシブ対応
2. ブラウザでビューポート幅を狭めて崩れを確認し、必要なら `PdfPreview.tsx` 側を調整
