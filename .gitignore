# OS
.DS_Store
Thumbs.db

# Environment variables & secrets
.env
.env.local
.env.*.local

# Claude local overrides (settings.json はチェックイン対象、local.json は個人用)
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# Node / Vite
node_modules/
dist/
tsconfig.tsbuildinfo
# グローバル gitignore が package-lock.json を除外するため、このリポでは明示的に追跡する（npm ci / Dependabot に必要）
!package-lock.json

# 生成物・共有フォルダ（実行時に作られる）
generated/
shared/

# SQLite Databases
*.db
*.sqlite
*.sqlite3

